/**
 * Scrape TikTok for trending hashtags and audios.
 */
export async function fetchTrending(): Promise<{ hashtags: string[]; audios: string[] }> {
  // TODO: Use Browserless/Puppeteer to scrape TikTok trending page
  return { hashtags: [], audios: [] };
}
