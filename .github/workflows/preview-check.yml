name: Preview Checks
on: [pull_request]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: node -v
      - name: Get preview URL
        id: preview
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          url=""
          for i in {1..20}; do
            url=$(gh api repos/${{ github.repository }}/deployments --jq '.[] | select(.environment=="Preview") | .payload.web_url' 2>/dev/null | head -n 1 || true)
            if [ -n "$url" ] && [ "$url" != "null" ]; then
              echo "url=$url" >> $GITHUB_OUTPUT
              break
            fi
            sleep 15
          done
      - name: Smoke test
        if: steps.preview.outputs.url != ''
        run: |
          url=${{ steps.preview.outputs.url }}
          code=$(curl -s -o /dev/null -w "%{http_code}" "$url/api/ping")
          if [ "$code" -ne 200 ]; then
            echo "Failed /api/ping: $code"
            exit 1
          fi
      - name: Preview unavailable
        if: steps.preview.outputs.url == ''
        run: echo 'No preview URL yet'
