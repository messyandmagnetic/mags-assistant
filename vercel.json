{
  "version": 2,
  "builds": [
    { "src": "next.config.js", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ],
  "crons": [
    { "path": "/api/cron/drive?key=cron", "schedule": "*/5 * * * *" }
  ]
}
